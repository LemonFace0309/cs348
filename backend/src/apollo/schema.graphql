type Query {
  user(username: String!): User
  users(usernames: [String!]!): [User]!
}

type User {
  name: String!
  username: String!
  createdAt: String
  followersCount: Int
  followingCount: Int
  tweetCount: Int
}